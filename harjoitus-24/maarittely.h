#include <iostream>
#include <fstream>
#include <string>

using namespace std;

const int MAX_PERSON = 9; // Max count for people in register

struct person {
	string firstname;
	double distschool;
	int sizehat;
};